<UserControl x:Class="WpfApp.Views.ProdutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <GroupBox Grid.Row="0" Header="Filtros">
            <StackPanel Orientation="Horizontal">
                <Label Content="Nome:" Width="60"/>
                <TextBox Width="220" Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
                <Label Content="Código:" Width="60" Margin="12,0,3,0"/>
                <TextBox Width="140" Text="{Binding FiltroCodigo, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
                <Label Content="Valor de:" Width="65" Margin="12,0,3,0"/>
                <TextBox Width="90" Text="{Binding ValorMin, UpdateSourceTrigger=PropertyChanged, Delay=300, TargetNullValue=''}"/>
                <Label Content="até:" Width="35"/>
                <TextBox Width="90" Text="{Binding ValorMax, UpdateSourceTrigger=PropertyChanged, Delay=300, TargetNullValue=''}"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Dados do Produto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>

                <Label   Grid.Column="0" Content="ID:"/>
                <TextBox Grid.Column="1" Style="{StaticResource ReadOnlyTextBox}"
                         Text="{Binding EmEdicao.Id, Mode=OneWay}"/>
                <Label   Grid.Column="2" Content="Nome:"/>
                <TextBox Grid.Column="3" Text="{Binding EmEdicao.Nome, UpdateSourceTrigger=PropertyChanged}"/>
                <Label   Grid.Column="4" Content="Código:"/>
                <TextBox Grid.Column="5" Text="{Binding EmEdicao.Codigo, UpdateSourceTrigger=PropertyChanged}"/>
                <Label   Grid.Column="6" Content="Valor:"/>
                <TextBox Grid.Column="7" HorizontalContentAlignment="Right"
                         Text="{Binding EmEdicao.Valor, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"/>
            </Grid>
        </GroupBox>


        <StackPanel Grid.Row="2" Style="{StaticResource ActionBar}">
            <Button Content="Incluir" Command="{Binding IncluirCommand}"/>
            <Button Content="Editar"  Command="{Binding EditarCommand}"/>
            <Button Content="Salvar"  Command="{Binding SalvarCommand}"  Style="{StaticResource PrimaryButton}"/>
            <Button Content="Excluir" Command="{Binding ExcluirCommand}" Style="{StaticResource DangerButton}"/>
        </StackPanel>


        <DataGrid Grid.Row="3" Margin="0,8,0,0"
                  ItemsSource="{Binding Produtos}"
                  SelectedItem="{Binding Selecionado}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"     Binding="{Binding Id}"                                Width="60"/>
                <DataGridTextColumn Header="Nome"   Binding="{Binding Nome}"                              Width="*"/>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}"                            Width="160"/>
                <DataGridTextColumn Header="Valor"  Binding="{Binding Valor, StringFormat=R$ {0:N2}}"     Width="140">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Margin" Value="0,0,8,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
